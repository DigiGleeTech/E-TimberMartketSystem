@model Rocky.Models.Lga

@{
    ViewData["Title"] = "CreateLga";
}

<h1>CreateLga</h1>

<h4>Lga</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateLga">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="LgaName" class="control-label"></label>
                <input asp-for="LgaName" class="form-control" />
                <span asp-validation-for="LgaName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StateId" class="control-label"></label>
                <select asp-for="StateId" class ="form-control" asp-items="@ViewBag.State"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
